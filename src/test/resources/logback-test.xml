<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="dilivia.s2" level="DEBUG" />
    <logger name="dilivia.s2.S2CellUnion" level="INFO" />
    <logger name="dilivia.s2.S2EdgeCrossings" level="INFO" />
    <logger name="dilivia.s2.S2EdgeDistances" level="INFO" />
    <logger name="dilivia.s2.S2RegionCoverer" level="INFO" />

    <logger name="dilivia.s2.S2CellUnionTest" level="INFO" />
    <logger name="dilivia.s2.S2RegionCovererTest" level="INFO" />

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>